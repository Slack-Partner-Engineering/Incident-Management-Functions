{
  "type": "shortcut",
  "name": "Create an Incident",
  "workflow": "#/workflows/post_incident_from_button_wf",
  "inputs": {
    "currentUser": {
      "value": "{{data.user_id}}"
    },
    "currentChannel": {
      "value": "{{data.channel_id}}"
    },
    "currentTime": {
      "value": "{{data.trigger_ts}}"
    },
    "interactivity_context": {
      "value": "{{data.interactivity}}"
    }
  }
}
