{
  "type": "shortcut",
  "name": "Create an Incident",
  "workflow": "#/workflows/postIncidentButtonWF",
  "inputs": {
    "currentUser": {
      "value": "{{data.user_id}}"
    },
    "currentChannel": {
      "value": "{{data.channel_id}}"
    },
    "currentTime": {
      "value": "{{event_timestamp}}"
    },
    "interactivity_context": {
      "value": "{{data.interactivity}}"
    }
  }
}
