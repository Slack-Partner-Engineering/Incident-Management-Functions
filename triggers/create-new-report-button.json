{
  "type": "shortcut",
  "name": "Incidents Report",
  "workflow": "#/workflows/getIncidentReportButtonWF",
  "inputs": {
    "currentUser": {
      "value": "{{data.user_id}}"
    },
    "currentChannel": {
      "value": "{{data.channel_id}}"
    },
    "currentTime": {
      "value": "{{event_timestamp}}"
    }
  }
}
